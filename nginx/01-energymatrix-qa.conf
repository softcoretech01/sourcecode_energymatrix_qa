server {
    listen 80;
    server_name qa.softcoretech.in;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name qa.softcoretech.in;

    ssl_certificate /etc/letsencrypt/live/qa.softcoretech.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/qa.softcoretech.in/privkey.pem;

    # 1. BACKEND API
    location /energymatrix/qa/api/ {
        # This removes '/energymatrix/qa/api/' so the Python app sees only the route (e.g., /auth/login)
        rewrite ^/energymatrix/qa/(.*)$ /$1 break;

        proxy_pass http://qa_backend:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
   # 2. FRONTEND REACT
    location /energymatrix/qa/ {
        # 'root' is more reliable than 'alias' for reloads
        root /usr/share/nginx/html;
        index index.html;
        # Try the file, then folder, then fallback to index.html
        try_files $uri $uri/ /energymatrix/qa/index.html;
    }

    # Remove the @qa_fallback block entirely as it's not needed with 'root'

    # New block to handle the React routing fallback correctly with alias
    location @qa_fallback {
        rewrite ^ /energymatrix/qa/index.html break;
    }

    # 3. Handle the URL without the trailing slash
    location = /energymatrix/qa {
        return 301 /energymatrix/qa/;
    }
}
